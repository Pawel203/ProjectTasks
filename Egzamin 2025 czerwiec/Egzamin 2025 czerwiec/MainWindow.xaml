<Window x:Class="Egzamin_2025_czerwiec.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Egzamin_2025_czerwiec"
        mc:Ignorable="d"
        Title="Szyfrowanie. Wykonane przez Paweł Owczarzak" Height="450" Width="800"
        Background="CadetBlue">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Margin="15" Foreground="AntiqueWhite" FontSize="20">Podaj wartość klucza</TextBlock>
            <TextBox Grid.Row="1" Margin="15" Width="50" x:Name="klucz"></TextBox>
            <TextBlock Grid.Row="2" Margin="15" Foreground="AntiqueWhite" FontSize="20">Podaj tekst</TextBlock>
            <TextBox Grid.Row="3" Margin="15" TextWrapping="Wrap" x:Name="tekst"></TextBox>
        </Grid>
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Margin="15" Foreground="AntiqueWhite" FontSize="20">Podaj wartość klucza</TextBlock>
            <Border 
                Grid.Row="1" 
                CornerRadius="20" 
                Background="CadetBlue" 
                BorderBrush="AntiqueWhite" 
                BorderThickness="3"
                Margin="15"
                Padding="20">
                <TextBlock Foreground="AliceBlue" FontSize="14" x:Name="zaszyfrowany_tekst"/>
            </Border>
            <Button Grid.Row="2" Margin="15" Width="200" Background="LightBlue" Click="zapisz">Zapisz szyfr w pliku</Button>
        </Grid>
        <Button Grid.Column="1" Width="100" Height="20" Background="LightBlue" Click="szyfruj">Szyfruj</Button>
    </Grid>
</Window>
